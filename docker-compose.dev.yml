version: "3"
services: 
  node-app:
    build:
      context: .
      args: 
        NODE_ENV: development
    image: node-app:dev1
    read_only: true
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev